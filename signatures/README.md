# Signatures

Do not commit local private keys here.

A local prototype public key may be shared next to a reviewed artifact only if the trust boundary is clearly described. A local prototype key is not a Whonix trust anchor and is useful only for checking whether the local prototype artifact changed after signing.

The public repository keeps this directory as documentation only.
