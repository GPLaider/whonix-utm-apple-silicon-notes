# Checksums

Do not commit local prototype tar files or VM artifacts here.

If a local prototype artifact is shared privately for review, provide:

- outer `.tar.SHA512SUM`
- detached `.tar.SHA512SUM.asc`
- internal `SHA512SUMS`
- internal `BUNDLE-SHA512SUMS`

The public repository keeps this directory as documentation only.
