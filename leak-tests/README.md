# Leak-test notes

This directory contains sanitized example leak-test summaries only.

Do not commit full private logs or VM state here. Keep timestamps, commands, and result categories, but omit host-local paths and unnecessary network identifiers.

Systemcheck/leak-test evidence is local evidence only. It does not prove full security or broad compatibility.
