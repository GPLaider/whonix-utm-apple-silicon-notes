# Smoke notes

This directory contains sanitized example smoke-test summaries only.

Do not commit full VM artifacts, full private logs, local UTM state, local paths, hostnames, or private key identity text here.

The current example summarizes one local Apple Silicon + UTM test pair and should not be read as a broader compatibility claim.
